services:
  - type: web
    name: mafia-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python mafia_bot.py"
    envVars:
      - key: DISCORD_TOKEN
        sync: false